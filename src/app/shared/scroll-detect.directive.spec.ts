import { ScrollDetectDirective } from './scroll-detect.directive';

describe('ScrollDetectDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollDetectDirective();
    expect(directive).toBeTruthy();
  });
});
