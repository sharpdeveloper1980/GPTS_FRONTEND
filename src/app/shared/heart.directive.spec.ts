import { HeartDirective } from './heart.directive';

describe('HeartDirective', () => {
  it('should create an instance', () => {
    const directive = new HeartDirective();
    expect(directive).toBeTruthy();
  });
});
